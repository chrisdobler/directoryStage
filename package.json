{
  "name": "directoryStage",
  "version": "1.0.0",
  "description": "Receives a set of operation commands, and models a directory based on the commands.",
  "main": "cli.ts",
  "scripts": {
    "spec": "jest",
    "start": "nodemon --exec ts-node lib/cli.ts"
  },
  "author": "Chris Dobler",
  "license": "MIT",
  "dependencies": {
    "typescript": "^3.9.2"
  },
  "devDependencies": {
    "@babel/core": "^7.9.6",
    "@babel/node": "^7.8.7",
    "@babel/preset-env": "^7.9.6",
    "jest": "^26.0.1",
    "nodemon": "^2.0.4",
    "ts-node": "^8.10.1"
  }
}
